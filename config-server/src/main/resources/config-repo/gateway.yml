spring:
  cloud.gateway.routes:
    - id: accounts-route
      uri: lb://accounts
      predicates:
        - Path=/accounts/**
    - id: cash-route
      uri: lb://cash
      predicates:
        - Path=/cash/**
