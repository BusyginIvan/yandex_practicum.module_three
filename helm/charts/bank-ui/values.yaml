replicaCount: 1

image:
  repository: bank-ui
  tag: latest
  pullPolicy: IfNotPresent

containerPort: 8080

service:
  port: 80
  targetPort: 8080

probes:
  path: /actuator/health
  initialDelaySeconds: 30
  periodSeconds: 10
  timeoutSeconds: 5
