server.port: ${SERVER_PORT:8088}
spring:
  application.name: notifications
  config.import: configserver:${CONFIG_SERVER_URL:http://config-server:8081}
management.endpoints.web.exposure.include: health
