apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: keycloak-data
  annotations:
    "helm.sh/resource-policy": keep
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
